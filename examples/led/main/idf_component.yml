dependencies:
  idf: ">=5.4"
  espressif/cjson: "^1.7.19"
  # 1.4.0 avoids known build breaks with newer toolchains/dependency resolution
  # (mbedtls include path in esp_encrypted_img and C++ optional equality in closure-control).
  espressif/esp_matter: "==1.4.0"
  espressif/mqtt: "*"
